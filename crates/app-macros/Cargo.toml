[package]
name = "notespace-app-macros"
description = "The #[handler] attribute: an axum handler whose future is Send on both targets."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
